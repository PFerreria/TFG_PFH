{
  "$schema": "../node_modules/@tauri-apps/cli/schema/acl/capabilities/desktop.json",
  "identifier": "default",
  "description": "Default capabilities for the IMERS desktop app",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "shell:allow-execute",
    "shell:allow-spawn"
  ]
}
